'use strict';

const number = [1, 2, 3, 4, 5, 6];
let logItems = ['Mango', 'Poly', 'Ajax', 'Lux', 'Jay', 'Kong'];
// let logItems = [5, 10, 15, 20, 25, 30, 35, 40, 45, 50];

for (let i = 0; i < logItems.length; i++) {
    console.log(`${i + 1} - ${logItems[i]}`);
}